这是一个高难度、长时间运行的端到端复现任务。请为另一个任务创建一个完整、自包含的 goal，把执行工作交给它，并由当前任务持续监督。定期检查真实进展、日志和产物；如果它卡住、跳过步骤、误判成功或采用无效方案，主动诊断、补充指令并纠偏。不要只等待最终回复，也不要停留在计划或部分结果。

## 最终目标

在当前 Linux 服务器上完整复现并部署：

https://github.com/touhoufan2024/qlibAssistant.git

完成一次真实的端到端运行：

1. 使用指定的本地 Qlib 数据包
2. 验证并加载数据
3. 在干净目录中从零训练全部目标模型
4. 验证模型产物和评价指标
5. 执行模型筛选、加权集成和选股预测
6. 展示真实生成的结果
7. 将流程部署为可重复运行、可观察、失败可恢复的自动任务

用途仅限研究和技术验证；不得连接券商、自动下单或执行实盘交易。

## 固定数据输入

数据包已经存在：

`/data0/zhangpeng6/rsync/qlib_bin.tar.gz`

必须遵守：

- 不克隆或运行 `chenditc/investment_data`
- 不从 GitHub Release 重新下载数据
- 不调用 Tushare、Dolt 或其他上游数据生成流程
- 不修改或删除原始压缩包
- 计算并记录压缩包大小、修改时间和 SHA-256
- 先用压缩包测试命令检查归档完整性
- 解压到本任务专用的隔离目录，不覆盖用户现有 Qlib 数据
- 配置 `qlibAssistant` 使用该隔离数据目录
- 如果代码写死了 `~/.qlib/qlib_data/cn_data`，优先通过配置或启动参数覆盖；只有确实无法配置时才做最小代码修改

不要因为文件存在就直接认定数据有效。

数据验证至少包括：

- 压缩包完整且可解压
- Qlib 初始化成功
- calendar 文件存在且非空
- 输出数据中的最新交易日
- CSI300 instruments 非空
- 抽样读取若干股票的价格字段
- 抽样生成或读取 Alpha158 特征
- 输出股票数量、日期范围、最新交易日和异常情况
- 检查数据日期是否足以支持仓库当前训练配置

## 第一阶段：环境预检

执行前检查：

- 操作系统和 CPU 架构
- CPU 核数和可用内存
- 可用磁盘空间
- Python、Git 和必要系统工具
- `/data0/zhangpeng6/rsync/qlib_bin.tar.gz` 的读取权限
- 工作目录和模型目录的写入权限
- 长时间训练任务的可靠运行方式

克隆主仓库并记录实际分支和 commit SHA。阅读当前代码、README、`requirements.txt`、训练脚本和 GitHub Actions，以当前代码为事实来源，不盲目照抄 README。

建立隔离、可重复的 Python 环境。记录所有依赖的实际版本。

不得删除用户已有数据或模型。所有数据、模型、日志和分析结果都使用本次任务专用目录。

## 第二阶段：从零训练

训练必须使用：

- 本次从指定压缩包解压的数据
- 本次新建且训练前已证明为空的模型实验目录

禁止：

- 执行 `model decompress_mlruns`
- 使用仓库自带模型
- 下载预训练模型
- 复用已有 `~/.qlibAssistant/mlruns`
- 只训练单个模型后宣称完整复现
- 缩小数据范围或训练窗口来掩盖资源问题

以当前代码中的实际训练矩阵为准。预期至少覆盖：

- XGBoost
- Linear
- DoubleEnsemble
- LightGBM
- CatBoost
- Alpha158
- CSI300
- 每种算法对应1～5年历史窗口

预期为5种算法 × 5个窗口，共25个有效训练记录。如果当前代码的实际矩阵不同，必须指出具体代码位置并解释差异。

训练要求：

1. 启动真实训练并持续监控。
2. 使用断线后仍能运行的方式执行长任务。
3. 保存每个算法和窗口的日志、开始时间、结束时间和退出状态。
4. 监控 CPU、内存、磁盘和异常退出。
5. 训练脚本即使捕获错误后继续，也不能据此判定整批成功。
6. 逐项验证训练记录；失败后定位根因，只重跑失败项。
7. 不重复已经成功的训练。
8. 必要时根据实际 CPU 核数调整线程数，避免内存耗尽，但不得减少模型或窗口数量。
9. 每个有效 recorder 至少应包含 `params.pkl`、`sig_analysis` 和训练任务信息。
10. 输出每个 recorder 的 IC、ICIR、Rank IC、Rank ICIR。
11. 检查 NaN、空预测、损坏模型和异常指标。

## 第三阶段：模型筛选和选股

训练完成后，使用本次从零训练产生的模型运行：

- 模型列表和完整性检查
- 模型指标过滤
- 集成权重计算
- `model selection`
- 最新可用交易日的选股预测

禁止使用预训练模型补足失败项。

验证分析目录中真实生成以下文件或当前版本的等价产物：

- `total.csv`
- `total.md`
- 日期对应的 `*_ret.csv`
- 日期对应的 `*_filter_ret.csv`

结果报告必须包含：

- 数据最新交易日
- 实际预测日期
- 成功训练的算法和窗口
- 有效实验及 recorder 数量
- 每个模型的 IC、ICIR、Rank IC、Rank ICIR
- 模型筛选结果和集成权重
- 未过滤选股结果前10名
- 稳健性过滤后结果前10名
- 股票代码、名称、`avg_score`、`pos_ratio` 和相关过滤字段
- 所有结果文件的绝对路径

如果过滤后没有股票，必须如实报告并说明触发了哪些过滤条件，不得伪造候选结果。

如果最新预测日期尚无未来真实收益，允许 `real_label` 为空，但必须说明这是预测时点造成的。

## 第四阶段：自动化部署

首次完整运行成功后，再整理自动化：

1. 提供一个一键运行入口。
2. 数据输入固定使用：
   `/data0/zhangpeng6/rsync/qlib_bin.tar.gz`
3. 每次运行先比较压缩包 SHA-256 或修改时间。
4. 仅当数据发生变化且数据最新交易日晚于模型日期时才重新训练。
5. 数据未变化时不得重复进行昂贵训练。
6. 增加互斥锁，防止多个训练任务同时运行。
7. 增加磁盘空间检查、结构化日志和明确失败退出码。
8. 支持断点续训，只重跑失败或缺失项。
9. 根据服务器环境选择 systemd timer、cron 或已有调度系统。
10. 记录时区和实际触发时间。
11. 定时部署完成后进行一次安全的手动触发验证。
12. 提供启动、停止、查看进度、查看日志、查看结果、重跑失败项和禁用定时任务的方法。

不要自动 Git push、上传结果或向外部服务发送数据。

## 验收标准

只有全部满足后才能宣布完成：

- 仓库 commit SHA 和依赖版本已记录
- 指定压缩包已完成完整性及 SHA-256 验证
- 数据解压到隔离目录并通过 Qlib 读取验证
- 模型目录在训练前已证明为空
- 目标训练矩阵经过真实执行
- 每个成功训练项都有有效模型和分析产物
- 批处理中的隐藏失败已经逐项排除
- selection 使用的是本次从零训练的模型
- 已生成并展示真实选股 CSV
- 自动化入口已经部署并完成实际验证
- 提供完整日志、结果路径和故障恢复方法
- 未使用任何预训练模型
- 未伪造、推测或手工填写选股结果

## 监督规则

当前任务必须持续 babysit 执行任务：

- 定期检查进程、资源、日志和最新产物
- 区分正常的长时间训练与真正卡死
- 发现错误后发送具体诊断和下一步指令
- 恢复时复用成功产物，只重跑失败项
- 不因一个命令返回0就宣布成功
- 不因训练时间长而退化为只写文档
- 不因部分模型成功而宣布全流程成功
- 除非需要危险权限、付费资源或可能破坏现有数据，否则不要把常规技术决策抛回给我
- 只有所有验收标准满足，或者存在有证据且必须由我处理的真实阻碍时，才结束监督

## 最终报告

最终提交一份证据化报告：

1. 完成或未完成结论
2. 服务器环境和仓库 commit SHA
3. 数据包路径、大小、修改时间、SHA-256及最新交易日
4. 实际训练矩阵和每项状态
5. 失败、重试和修复记录
6. 模型指标、筛选结果和集成权重
7. Top 10未过滤及过滤后选股结果
8. 模型、结果和日志的绝对路径
9. 所有本地代码修改及原因
10. 一键重跑、查看状态和恢复失败任务的方法
11. 定时自动化状态
12. 尚存限制或真实阻碍

报告中的每一个“成功”都必须由日志、命令输出或真实产物支持。
