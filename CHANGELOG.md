# Changelog

本项目的重要变更记录在此文件中。

## [Unreleased]

## [2026-07-24]

### Added

- 新增稳健集成后处理：
  - 将每个模型的原始预测转换为当日 CSI300 横截面百分位排名；
  - 使用 Rank IC 权重与等权各占一半的收缩权重；
  - 输出 `ensemble_metadata.json`，记录方法、门槛和最终模型权重。
- 历史复盘新增逐股明细，可按日期展开查看 Top10 股票的预测分、模型看涨
  占比、实际收益和盈亏状态。
- Dashboard 新增五类模型的训练任务进度、日志摘要和实时资源占用。
- 新浪增量更新新增 `SH000300` 指数抓取、回放校验和 staging 验证。
- 新增 `--repair-benchmark`，用于安全回补旧版增量更新遗漏的指数数据。
- 新增稳健集成的门槛、权重和尺度归一化单元测试。

### Changed

- 模型准入的 Rank ICIR 下限由 `0.001` 提高到 `0.10`。
- 流水线在生成或复用选股结果后统一执行稳健集成，保证重复执行结果一致。
- `avg_score` 改为约 `[-0.5, 0.5]` 范围内的相对排名集成分数，不再解释为
  可直接交易的预期收益率。
- 历史复盘统计产物现在包含每个组合的逐股结果。

### Fixed

- 修复新浪只补充成分股、未补充 `SH000300`，导致基准收益和超额收益为空的
  问题。
- 修复不同模型输出尺度不一致时，波动更大的模型获得隐性额外权重的问题。

### Security

- Dashboard 增加 HTTP Basic 认证，密码使用 PBKDF2-SHA256 摘要验证。
- Dashboard 凭据文件 `.dashboard.env` 明确排除在 Git 跟踪之外。
